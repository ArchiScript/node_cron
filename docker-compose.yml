version: "3.8"

services:
  scraper:
    image: ghcr.io/archiscript/cronjobs-container:latest
    networks:
      - ht-net
    secrets:
      - ghcr_token
    deploy:
      replicas: 1
secrets:
  ghcr_token:
    external: true

networks:
  ht-net:
    external: true
